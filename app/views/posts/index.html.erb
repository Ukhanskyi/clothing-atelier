<h1> All posts </h1>
<% @post.each do |post| %>
  <hr>
  <div class="alert alert-light">
    <h3><%= post.title %></h3>
    <p><%= post.body %></p>
    <%= link_to "Read next", post_path(post), :class => 'btn btn-outline-primary'  %>
  </div>
<% end %>