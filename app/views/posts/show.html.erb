<h1> <%= @post.title %></h1>
<p> <%= @post.body %></p>
<hr>
<%= link_to "Edit", edit_post_path(@post), :class => 'btn btn-outline-warning' %>
<%= link_to "Destroy", post_path(@post), method: :delete, data: {confirm: "Are you sure?"}, :class => 'btn btn-outline-danger' %>